<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mjlabios.animasampleanimations">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CustomClass"
            android:label="@string/app_name" >

        </activity>

        <activity
            android:name=".SampleFlush"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".SamplePulse"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".SampleRotate"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".SampleShake"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".SampleFadeIn"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".SampleFadeOut"
            android:label="@string/app_name" >
        </activity>


    </application>

</manifest>
